# Repository presentation

Suggested description:

> Switch your Mac’s default email app in a few clicks. Set email links and saved messages to the apps you choose. Native, private and open source.

Suggested topics:

`macos`, `swift`, `appkit`, `email`, `outlook`, `apple-mail`, `mailto`, `eml`,
`msg`, `default-app`, `open-source`, `macos-utility`

The source uses a neutral project copyright holder. Maintainers should use a
private/noreply email for Git commits, enable GitHub secret scanning/push
protection where available, and enable private vulnerability reporting in the
repository settings. Do not publish local security reports that contain absolute
paths, workstation details or unrelated private context.
